import { PlaceholderPage } from "@/app/PlaceholderPage";

export function BackupPage() {
  return (
    <PlaceholderPage
      title="Datensicherung"
      phase="Phase 7 — Backup, Export und Restore"
      description="JSON-Vollexport, CSV-/Excel-Export sowie validierte Wiederherstellung (Voll-/Merge-Restore) folgen in Phase 7 (siehe BACKUP_AND_RESTORE.md)."
    />
  );
}
